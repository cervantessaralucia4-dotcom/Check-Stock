[phases.setup]
nixPkgs = ["php82", "php82Extensions.pdo", "php82Extensions.pdo_mysql", "php82Extensions.mysqli", "php82Extensions.mbstring", "php82Extensions.json"]

[phases.build]
cmds = ["echo 'build ok'"]

[start]
cmd = "php -S 0.0.0.0:$PORT -t public"
```

**Paso 3** — En Railway → tu servicio → **Variables** → agrega esta variable nueva:
```
RAILPACK_BUILD_STRATEGY = nixpacks
